package br.com.sicredi.associado.services;

red;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import br.com.sicredi.associado.dtos.AssociadoDTO;
import br.com.sicredi.associado.entities.Associado;
import br.com.sicredi.associado.repositories.AssociadoRepository;

@Service
public class AssociadoService {

	@Autowired
	private AssociadoRepository associadoRepository;

	public o insert(AssociadoDTO objDto) {
		Associado obj = new Associado(objDto.getId(), objDto.getNome(), objDto.getCpf());
		obj = associadoRepository.save(obj);
		PautaDTO result = new PautaDTO(obj.getId(), obj.getNome());
		return result;
	}

}
